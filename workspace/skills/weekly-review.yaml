name: Weekly Review
schedule: "30 10 * * 0"
permissionProfile: scheduled
validityWindowMinutes: 60
promptTemplate: |
  {{personality}}

  Today is {{dayOfWeek}}, {{date}}.

  Generate a weekly review summarizing what the user did this past week (Monday through Saturday plus Sunday morning — this runs Sunday at 10:30, so the current Sunday is only partially covered). Write it to an Obsidian note and send a Telegram summary.

  ## Step 1 — Gather the week's data

  **Journal entries:**
  Daily journal notes are stored in year/month folders: `~/dev/notes/remotevault/personal/journal/YYYY/MM/YYYY-MM-DD.md`
  Read this week's entries (`-newermt` takes a time expression directly — do not pass a date through `-newer`, which expects a reference *file*):
  ```
  find ~/dev/notes/remotevault/personal/journal -path '*/[0-9][0-9][0-9][0-9]/[0-9][0-9]/*.md' -newermt '7 days ago' 2>/dev/null | sort
  ```

  **Cortex memory:**
  Run `/recall` with queries: "this week", "decision", "shipped", "built", "fixed", "learned" to surface key events.

  **Git activity (via GitHub API):**
  The user codes on a local machine and pushes to GitHub — check there for the authoritative commit history.

  Read `~/dev/claude-plugins/reclaw/workspace/skills/SHARED_GIT_QUERIES.md`
  and follow the procedure with parameters:
  - `CUTOFF=$(date -d '7 days ago' --iso-8601=seconds)`
  - `PER_PAGE=50`
  - `OUTPUT_FMT=messages`

  Run Step A → Step B for each active repo → Step C for open PRs. Group
  commits by branch in the summary; include feature-branch work even
  without a PR — it represents real effort.

  **Calendar events this week:**
  Compute the date 7 days ago from {{date}}, then:
  ```
  bun /home/peterstorm/dev/claude-plugins/reclaw/scripts/calendar-events.ts YYYY-MM-DD {{date}}
  ```
  (replace first YYYY-MM-DD with 7 days ago). Returns JSON array of events for the range.

  ## Step 2 — Write the weekly review note

  Create a note at: `~/dev/notes/remotevault/personal/journal/weekly/YYYY-WNN.md` (e.g. `2026-W09.md`)
  Create the `personal/journal/weekly/` directory if it doesn't exist.

  **Frontmatter:**
  ```yaml
  ---
  title: "Weekly Review — YYYY-WNN"
  date: YYYY-MM-DD
  tags: [journal, weekly-review]
  up: "[[personal/journal/MOC]]"
  ---
  ```

  **Sections:**
  - `## Highlights` — 3-5 most notable things (shipped features, key decisions, interesting conversations)
  - `## What I worked on` — grouped by project/area (reclaw, dotfiles, work, learning, etc.)
  - `## Reflections` — patterns you notice: what went well, what was frustrating, any recurring themes from journal entries
  - `## Next week` — carry-forward items, unfinished threads, upcoming calendar events

  Be concrete — reference specific commits, notes, and conversations. Don't be generic.

  ## Step 3 — Store in cortex

  Run `/remember`: "Weekly review {{date}}: [2-sentence summary of the week's highlights]"

  ## Step 4 — Send Telegram summary

  Send a concise version — not the full note, just the highlights.

  **Format:**
  ```
  📋 Weekly Review — Week NN

  Highlights:
  • [highlight 1]
  • [highlight 2]
  • [highlight 3]

  Themes: [1-sentence reflection]

  Full review saved to vault. Swipe-reply to discuss anything.
  ```
