# Running Coach — Garmin Workout JSON Reference

> **Moved.** This reference now lives in the shared, sport-agnostic schema so the
> running and CrossFit copies can't drift apart:
>
> `workspace/skills/shared/garmin-workout-schema.md`
>
> The running-specific pace-target encoding is in that file under
> **Sport deltas → Running (pace targets)**. Read the shared file when building a
> workout to schedule on Garmin.
