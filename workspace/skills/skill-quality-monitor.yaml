name: Skill Quality Monitor
schedule: "0 17 * * 0"
permissionProfile: scheduled
validityWindowMinutes: 60
timeout: 300
promptTemplate: |
  {{personality}}

  Time: {{dayOfWeek}}, {{date}}

  You are the skill quality monitor. Reclaw records anomalies (only) from
  scheduled skill runs as JSON lines in a durable log at
  `~/.cache/reclaw/skill-quality.jsonl`. Your job is to surface degradation
  patterns from those signals.

  **Suppress when healthy.** If nothing is degrading, respond with exactly
  ALL_CLEAR — no backticks, no formatting, nothing else. The runtime suppresses
  that and the user is not paged.

  ## Data model

  Each line is a JSON object written by `src/infra/skill-quality.ts`:

  ```json
  {"timestamp":"<ISO8601>","skillId":"<id>","status":"suppressed|claude_error|skill_not_found","durationMs":<n>,"outputLength":<n>,"errorMessage":"<string|null>","severity":<5|7|8>,"summary":"<human one-liner>"}
  ```

  - `status=suppressed` (severity 5) — the skill produced ALL_CLEAR / no output
    (e.g. tech-digest with no feeds reachable).
  - `status=claude_error` (severity 7) — the subprocess failed; `errorMessage` has the reason.
  - `status=skill_not_found` (severity 8) — scheduler referenced a skill missing from
    the registry (config drift).

  Plain successes are NOT recorded. Absence of data for a skill = healthy.
  Do not try to compute success rates — you can't from this dataset.

  ## Step 1 — Pull signals from the last 7 days

  Read the log and filter to the 7-day window (the file is append-only and never
  pruned, so old lines accumulate — always filter by timestamp):

  ```bash
  bun -e "
  import { readFileSync } from 'fs';
  import { homedir } from 'os';
  import { join } from 'path';
  const path = join(homedir(), '.cache/reclaw/skill-quality.jsonl');
  let raw = '';
  try { raw = readFileSync(path, 'utf8'); } catch { console.log('NO_LOG_FILE'); process.exit(0); }
  const cutoff = Date.now() - 7*24*60*60*1000;
  const rows = raw.split('\n').filter(Boolean)
    .map(l => { try { return JSON.parse(l); } catch { return null; } })
    .filter(r => r && Date.parse(r.timestamp) >= cutoff)
    .sort((a,b) => b.timestamp.localeCompare(a.timestamp));
  console.log('signals in 7d window:', rows.length);
  for (const r of rows) console.log('[' + r.status + '] ' + r.timestamp + '  ' + r.summary);
  "
  ```

  ### Step 1b — Distinguish "healthy" from "no signal"

  - **Signals found (count > 0):** proceed to Step 2.
  - **Zero signals (or `NO_LOG_FILE`):** decide whether the system was idle or the
    recorder is broken. Check the service log for scheduled runs in the window:

    ```bash
    journalctl --user -u reclaw --since "7 days ago" 2>/dev/null | grep -c "worker:scheduled"
    ```

    - Count is 0 → genuinely idle → respond ALL_CLEAR.
    - Count > 0 → scheduled skills ran but produced no anomalies. Recording is
      anomalies-only, so this is normally the healthy state. Confirm the
      recorder still writes (a silent write failure looks identical to health):

      ```bash
      journalctl --user -u reclaw --since "7 days ago" 2>/dev/null | grep "skill-quality.*failed to append"
      ```

      - Error lines present → recorder write-path is degraded. Alert with:
        "skill-quality recorder failing — append errors in the log; check
        `src/infra/skill-quality.ts` and `~/.cache/reclaw/` writability".
      - No error lines, but `NO_LOG_FILE` and scheduled runs happened → the log was
        never created; flag it as a possible wiring regression.
      - No error lines and the file exists → healthy → respond ALL_CLEAR.

  ## Step 2 — Group and triage

  Group entries by skill id. For each skill, count by status (suppressed /
  claude_error / skill_not_found) within the last 7 days. Apply judgment:

  **Always alert on:**
  - Any `skill_not_found` entry — scheduler references a skill that is not
    in the registry. Config drift, fix urgently.
  - 3+ `claude_error` entries for the same skill in 7 days — pattern, not
    a flake. Include the most recent error reason verbatim.

  **Use judgment for `suppressed`:**
  - Skills whose *purpose* is to alert only when something is wrong
    (homelab-watchdog, anything with ALL_CLEAR semantics by design) —
    persistent suppression is the healthy state. Do NOT alert.
  - Skills that *should* produce output every run (morning-briefing,
    tech-digest, evening-journal, hardware-intel) — 3+ consecutive
    suppressions in 7 days is a degradation signal (feed problem, API down,
    upstream data gap). Alert with the count.

  When in doubt about a skill's intent, read its yaml at
  `/home/peterstorm/dev/claude-plugins/reclaw/workspace/skills/<skill-id>.yaml`
  and look at the prompt — does it have an explicit `ALL_CLEAR` instruction
  for the healthy path, or is it expected to produce output?

  ## Step 3 — Telegram message (only if degradation found)

  Format tightly. One section per affected skill, ordered by severity
  (skill_not_found > repeated errors > persistent suppression).

  ```
  ⚠️ Skill quality alert — {{date}}

  <skill-id> — <count> errors in 7 days
    Latest: <most recent error reason, truncated to ~80 chars>
    Suggested: <one short concrete next step, e.g. "check Garmin auth", "review feed list">

  <skill-id> — <count> consecutive suppressions
    Likely upstream/data issue. <Concrete check, e.g. "tech-digest: 3+ feeds may be down">
  ```

  Keep it dense. No filler. The user reads this on their phone.

  ## Step 4 — Record findings (only on alerts)

  If you sent an alert, run `/remember` so future runs can spot recurring
  problems:

  `/remember "Skill quality {{date}}: <one-line summary of what's degrading>" --type=gotcha --tags=skill-quality-monitor,reclaw,degradation`

  Do NOT remember on `ALL_CLEAR` — adds noise without value.

  ## Step 5 — Healthy path

  If no skills meet the alert thresholds, respond with exactly:

  ALL_CLEAR
